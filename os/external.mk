# EpilykosOS Buildroot external tree.
#
# Nothing to build here directly — this file only needs to exist so
# Buildroot recognises os/ as a BR2_EXTERNAL tree. Board-specific
# post-build/post-image logic lives under board/epilykos/<board>/.
